class Solution(object) :
  def climbStair(self, n) :

    if n == 1 :
       #　階段が一段の時、登り方は一通りなので1を返す
      return 1

    # ways(1) = 1通り, ways(2) = 2通り をそれぞれ変数a, bに代入
    a = 1
    b = 2
    #3段目からn段目までをループ
    for _ in range(3, n + 1) :

      # 次の登り方の数は前の2つの和になる（フィボナッチ数列の関係）
      # a, b をそれぞれ更新していく（aは前の値、bは現在の値）
      a, b = b, a+b

#n段目までの総数を出す。(最後のbに結果が入ってる)
    return b
